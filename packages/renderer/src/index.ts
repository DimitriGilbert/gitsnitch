export interface PackageMetadata {
  readonly name: "@git-snitch/renderer";
  readonly role: "renderer";
  readonly version: "0.0.0";
}

export const rendererPackageMetadata = {
  name: "@git-snitch/renderer",
  role: "renderer",
  version: "0.0.0",
} satisfies PackageMetadata;
